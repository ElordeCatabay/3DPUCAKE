// src/index.js
import React from 'react';
import ReactDOM from 'react-dom';
// import './index.css';  // Comment out or remove this line if not using
import App from './App';
// import reportWebVitals from './reportWebVitals';  // Comment out or remove this line if not using

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// reportWebVitals();  // Comment out or remove this line if not using
